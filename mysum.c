#include <stdio.h>
int main()
{

    int a,b,c;
     printf("Type a number a: ");
     scanf("%d", &a);
     printf("Type a number b: ");
  scanf("%d",&b);
    c=a+b;

      printf("sum of both a and b is : %d", c);

return 0;
}
